@org.springframework.modulith.ApplicationModule(
        allowedDependencies = {
                "common",
                "common::error"
        }
)
package io.github.gseobi.commerce.orchestration.notification;
